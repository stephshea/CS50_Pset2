#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main(int argc, string argv[])
{
string s= [];
string initials;

string s = get_string("before: ");
    printf("after:  ");


    s = get_string("name: ");
    printf("%s\n", s);

    for (int i = 0, n <= strlen(s); i++)
    intials (toupper(s[i]));

    printf(initials, "\n");


}
  {
        if (islower(s[i]))
        {
            printf("%c", toupper(s[i]));
        }
        else
        {
            printf("%c", s[i]);
        }